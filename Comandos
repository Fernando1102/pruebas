Comandos

#En la carpeta que vayamos a usar siempre es recomendable 
git init

# Ver el estado del repo y saber cuales esatn en el staging area
git status

# ver la carpeta en la que estamos usando bash
pwd

#para hacer el commit
git commit -m "cambio1"

#Para ver la información de todos los commit
git log

#para  ver el último comit o cambiarnos de commit 
git checkout [nombre de la rama o del commit]

#para borrar los commit el git reset mas simple y que no toca niestro Working area (nuestro codigo)
git reset --soft [nombre del comit]

#para borrar los commit el git reset mas simple y que no toca niestro Working area y staging area.
git reset --mixed [nombre del commit]

#para borrar ltodo lo que hay en el commit
git reset --hard [nombre del commit]

#para genererar un archivo del log
git log > commits.txt


#Cambio realizado para ver lo de guardar en ramas

# segundo cambio realizado en la rama para ver la diferencia